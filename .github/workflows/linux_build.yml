name: Linux Build

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v6

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: 'stable'
          cache: true

      - name: Cache Pub Dependencies
        uses: actions/cache@v5
        with:
          path: ~/.pub-cache
          key: ${{ runner.os }}-pub-${{ hashFiles('**/pubspec.lock') }}
          restore-keys: |
            ${{ runner.os }}-pub-

      - name: Cache System Dependencies
        uses: actions/cache@v5
        with:
          path: /var/cache/apt/archives
          key: ${{ runner.os }}-apt-${{ hashFiles('.github/workflows/linux_build.yml') }}
          restore-keys: |
            ${{ runner.os }}-apt-

      - name: Install System Dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y clang cmake ninja-build pkg-config libgtk-3-dev liblzma-dev libfuse2 libsecret-1-dev libjsoncpp-dev

      - name: Install Project Dependencies
        run: flutter pub get

      - name: Build Linux
        run: flutter build linux --release

      - name: Install UPX
        run: sudo apt-get install -y upx

      - name: Compress with UPX
        run: upx --best --lzma build/linux/x64/release/bundle/canto_sync

      - name: Create AppImage
        run: |
          # Install linuxdeploy
          wget https://github.com/linuxdeploy/linuxdeploy/releases/download/continuous/linuxdeploy-x86_64.AppImage
          chmod +x linuxdeploy-x86_64.AppImage

          # Prepare AppDir
          mkdir -p AppDir/usr/bin
          mkdir -p AppDir/usr/lib
          mkdir -p AppDir/usr/share/applications
          mkdir -p AppDir/usr/share/icons/hicolor/scalable/apps

          # Copy binaries and resources
          cp -r build/linux/x64/release/bundle/* AppDir/usr/bin/
          
          # Setup icon and desktop file
          # Use SVG logo
          cp assets/logo.svg AppDir/usr/share/icons/hicolor/scalable/apps/cantosync.svg
          
          # Create .desktop file
          cat > AppDir/usr/share/applications/cantosync.desktop <<EOF
          [Desktop Entry]
          Name=CantoSync
          Exec=canto_sync
          Icon=cantosync
          Type=Application
          Categories=Audio;AudioVideo;Player;
          Terminal=false
          EOF

          # Run linuxdeploy
          ./linuxdeploy-x86_64.AppImage --appdir AppDir --output appimage --icon-file assets/logo.svg --desktop-file AppDir/usr/share/applications/cantosync.desktop

          # Rename for upload
          mv CantoSync-*.AppImage CantoSync-Linux.AppImage

      - name: Update Nightly Release
        uses: softprops/action-gh-release@v2
        if: github.event_name == 'push'
        with:
          name: "Nightly Build"
          tag_name: nightly
          files: CantoSync-Linux.AppImage
          prerelease: true
          body: "Auto-generated nightly build from commit ${{ github.sha }}."
